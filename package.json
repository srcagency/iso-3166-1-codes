{
	"name": "iso-3166-1-codes",
	"version": "1.2.0",
	"description": "Just the codes, nothing more",
	"repository": "srcagency/iso-3166-1-codes",
	"scripts": {
		"test": "node test",
		"build": "csv-parser < data.csv | jq -s -c > data.json"
	},
	"devDependencies": {
		"tape": "^5.0.1"
	},
	"license": "MIT"
}
